Скрипт мониторинга ресурсов раз в N (по умолчанию 10) секунд.

Использование:
```
# При запуске без указания файла лога, используется стандартный файл monitor.log
./script.sh
# Чтобы указать другой путь к файлу лога, его нужно передать аргументом
./script.sh [файл_лога]
```
